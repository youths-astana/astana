# Youths · Астана

Каталог клубов для подростков Астаны: https://youths-astana.github.io/astana/

Статический сайт: HTML, CSS и JavaScript, без сервера и без сборки. Все файлы лежат рядом, без папок.

## Файлы

| Файл | Что внутри |
|---|---|
| `index.html` | Страница сайта |
| `styles.css` | Оформление |
| `data.js` | **Клубы и секции.** Сюда добавляются новые клубы |
| `i18n.js` | Тексты на русском, казахском и английском |
| `config.js` | **Настройки сайта** (см. ниже) |
| `app.js` | Логика сайта |

## Как добавить клуб

1. Откройте сайт, внизу нажмите «+ Панель создателя».
2. Заполните форму, поставьте точку на карте, нажмите «Добавить в каталог».
3. Внизу панели нажмите «Скопировать» у блока с кодом.
4. На GitHub откройте `data.js` → карандаш (Edit).
5. Вставьте скопированное между `const CLUBS = [` и `];`. Если клубов несколько, между ними нужна запятая.
6. Нажмите «Commit changes». Через 1–2 минуты клуб появится на сайте у всех.

Клуб, добавленный через панель, но не перенесённый в `data.js`, виден только в вашем браузере.

## Настройки

Настройки меняются в файле `config.js`, а не здесь. Этот README сайт не читает.

- `siteUrl` — адрес сайта. Нужен для ссылок и QR-кодов.
- `orgFormUrl` — ссылка на Google Форму для организаторов клубов.
- `reportEmail` — почта, куда приходят сообщения «Информация устарела?».
- `goatcounter` — код анонимной статистики GoatCounter. Пусто — статистика выключена.
